export enum NeedAuth {
  HOME_AUTH = '/home',
  USERS = '/users',
  USER = '/users/[id]',
  PROFILE = '/profile',
  ABOUT = '/about',
}

export enum NoAuth {
  LOGIN = '/login',
  REGISTER = '/register',
  HOME = '/',
}

export enum Pages {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  HOME_AUTH = '/home',
  USERS = '/users',
  USER = '/users/[id]',
  PROFILE = '/profile',
  ABOUT = '/about',
}
